import { AddSession, GetSession } from './factory'
import { Get, GetAll, Add, AddEntities, Update } from './rest'

export let Factory = {
  AddSession,
  GetSession
}

export let Rest = {
  Get,
  GetAll,
  Add,
  AddEntities,
  Update
}
